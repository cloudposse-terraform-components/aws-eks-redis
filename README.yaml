name: "aws-eks-redis"
# Canonical GitHub repo
github_repo: "cloudposse-terraform-components/aws-eks-redis"
# Short description of this project
description: |-
  This component installs `redis` for EKS clusters. This is a Self Hosted Redis Cluster installed on EKS.
usage: |-
  **Stack Level**: Regional

  Use this in the catalog or use these variables to overwrite the catalog values.

  `stacks/catalog/eks/redis/defaults` file (base component for default Redis settings):

  ```yaml
  components:
    terraform:
      eks/redis/defaults:
        metadata:
          component: eks/redis
          type: abstract
        settings:
          spacelift:
            workspace_enabled: true
        vars:
          enabled: true
          name: redis
          tags:
            Team: sre
            Service: redis
          create_namespace: true
          kubernetes_namespace: "redis"
          # https://github.com/bitnami/charts/tree/master/bitnami/redis
          chart_repository: https://charts.bitnami.com/bitnami
          chart_version: "17.1.0"
          chart: "redis"
          timeout: 180
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          # Set a specific version of Redis using the image.tag and image.repository values.
          # Documentation: https://docs.bitnami.com/kubernetes/infrastructure/redis/configuration/change-image-version/
          # Defaults: https://github.com/bitnami/charts/blob/master/bitnami/redis/values.yaml#L81-L82
          chart_values:
            image:
              tag: 7.0.4-debian-11-r11
              repository: bitnami/redis
          # Disabling Manifest Experiment disables stored metadata with Terraform state
          # Otherwise, the state will show changes on all plans
          helm_manifest_experiment_enabled: false
  ```

  `stacks/catalog/eks/redis/dev` file (derived component for "dev" specific settings):

  ```yaml
  import:
    - catalog/eks/redis/defaults

  components:
    terraform:
      eks/redis/dev:
        metadata:
          component: eks/redis
          inherits:
            - eks/redis/defaults
        vars: {}
  ```

  <!-- prettier-ignore-start -->
  <!-- prettier-ignore-end -->
references:
  - name: cloudposse-terraform-components
    description: "Cloud Posse's upstream component"
    url: https://github.com/orgs/cloudposse-terraform-components/repositories
tags:
  - component/eks/redis
  - layer/data
  - provider/aws
  - provider/helm
# Categories of this project
categories:
  - component/eks/redis
  - layer/data
  - provider/aws
  - provider/helm
# License of this project
license: "APACHE2"
# Badges to display
badges:
  - name: Latest Release
    image: https://img.shields.io/github/release/cloudposse-terraform-components/aws-eks-redis.svg?style=for-the-badge
    url: https://github.com/cloudposse-terraform-components/aws-eks-redis/releases/latest
  - name: Slack Community
    image: https://slack.cloudposse.com/for-the-badge.svg
    url: https://slack.cloudposse.com
related:
  - name: "Cloud Posse Terraform Modules"
    description: Our collection of reusable Terraform modules used by our reference architectures.
    url: "https://docs.cloudposse.com/modules/"
  - name: "Atmos"
    description: "Atmos is like docker-compose but for your infrastructure"
    url: "https://atmos.tools"
contributors: [] # If included generates contribs
